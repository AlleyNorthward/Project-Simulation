&emsp;&emsp;@author 巷北  
&emsp;&emsp;@time 2026-03-25 20:05:54  

# 2026/3/25
## prim算法
本来其实还是阻塞着, 后遵循了降低启动难度的思想, 约定着`1h`内掌握, 于是便打开了书. 算法相关的内容是, 看了一遍书, 明确了作者的思想. 但是不打算看作者源代码, 而是按照自己的思想做一遍. 不过在初始的数据结构上存在问题. 具体而言就是对输入数据存储把握不准, 不知道如何遍历. 以前对于图的存储, 会选择邻接矩阵, 邻接表, 关联矩阵, 十分明确, 但是现在是含权值的图, 我想的是`map<vector<vextor<int>>, int>`, 但是会有自我怀疑, 而且不知道如何遍历. 想去瞅一眼作者写的, 但想了想, 还是把这个作为案例记录于此, 方便后续分析类似情形.
## idea
想了想, 图的相关数据结构, 其实还就是一个结构体. 结构体内部可以拓展出id, 权值等属性. 但是对于链表相关的, 不太熟悉, 也不敢去做. 整体大的循环轮次是边数, 也就是固定`n-1`次循环. 现在用的是`Edge`结构体, 存有属性`from`, `to`, `val`, 然后用`vector`存储. 思想来源还是因为嵌入式吧...
## 解决方案
